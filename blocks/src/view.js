// Frontend JavaScript for the Estimated Delivery Date block
document.addEventListener('DOMContentLoaded', function() {
    // Initialize any frontend functionality here
    const deliveryBlocks = document.querySelectorAll('.wp-block-ed-dates-ck-estimated-delivery');
    
    deliveryBlocks.forEach(block => {
        // Add any block-specific frontend functionality here
        // For example, updating the delivery date dynamically based on user interactions
    });
}); 